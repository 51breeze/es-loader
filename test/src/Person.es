package;
class Person{
    constructor(){
        
          document.addEventListener('DEVELOPMENT_HOT_UPDATE',(e)=>{

            console.log(  e.moduleClass === Home );

        });
    }
    start(){
        console.log("===Person==9999=======66666====");
        console.log( @Provider(server.User, index) );

        new Home();

    }
}
